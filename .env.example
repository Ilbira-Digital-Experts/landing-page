# Mismo proyecto Supabase compartido con fac (para ahorrar costes).
# Ilbira usa sus propias tablas prefijadas `ilbira_*` en el schema public,
# ver supabase/ilbira_schema.sql.
SUPABASE_URL=
SUPABASE_ANON_KEY=
